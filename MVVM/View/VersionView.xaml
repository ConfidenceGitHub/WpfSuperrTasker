<UserControl x:Class="WpfSuperrTasker.MVVM.View.VersionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfSuperrTasker.MVVM.View" 
             xmlns:viewmodel="clr-namespace:WpfSuperrTasker.MVVM.ViewModel" 
             d:DataContext="{d:DesignInstance Type=viewmodel:VersionViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodel:VersionViewModel></viewmodel:VersionViewModel>
    </UserControl.DataContext>

    <Grid>
        <Border>
            <GroupBox
    Margin="16"
    Header="Информация о версиях" 
    Style="{StaticResource MaterialDesignGroupBox}"
    UseLayoutRounding="True" FontSize="16">
                <TextBlock Text="{Binding Test}">
                    
                </TextBlock>
            </GroupBox>
        </Border>
    </Grid>

</UserControl>
